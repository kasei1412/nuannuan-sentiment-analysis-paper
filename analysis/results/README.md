# Results

This directory contains portfolio-safe outputs from the analysis.

- `figures/` contains charts and topic word clouds.
- `tables/` contains aggregate model metrics, topic summaries and comparison statistics.
- Row-level comments, usernames, error examples and serialized models are intentionally omitted.
